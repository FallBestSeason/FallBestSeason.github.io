autumn's notes:
FOR LIVE WORK (LOCAL):
  powershell:
  $ cd .\vue1\
  $ npm run serve

UPLOAD WORK TO GITHUB (DEV BRANCH):
  git bash in project folder
  $ git add .
  $ git commit -m ""
  $ git push -u origin main

DEPLOY CHANGES TO SITE (LIVE BRANCH):
  powershell:
  $ cd .\vue1\
  $ npm run build
  close powershell!

  git bash in project folder:
  $ git add dist && git commit -m ''
  $ git subtree push --prefix dist origin gh-pages

  ctrl + f5 site for updates!

og font color #2c3e50;
bg blue #5f9ea0;

todo
go from one manual tile in ProjectTiles.vue to progromatically generating them from an array
big gamer moves if you can make an array autopopulate with file names from the folder of project pages
implement switching to different pages in the projects tab, use code from app.vue / nav bar buttons